{% extends "base_sistema.html" %}
{% load staticfiles %}
{% load i18n %}
{% block extrahead %}
<style type="text/css">
    #img{
        width:50%;
        position:relative;
    }
    .img-responsive{
        border-radius:8px;
    }
    .lado{
        position:relative;
        display:inline-block;
    }
    #detalle{
        position:relative;
        bottom:5em;
        width:40%;
    }
    #detalle, #img{
        position:relative;
        display:inline-block;
    }
    #producto{
        text-align:center;
        font-size:3em;
        color: #fff;
        
        text-shadow: -1px -2px 10px #2196f3;
    }
    .pmo-contact{
        width:20%;
        margin:0 auto;
    }
    .compra, .venta{
        font-size:3em;        
    }
    .codigo{
        font-size:2.5em;
    }
    .ptib-item{
        font-size:1.2em;
    }
</style>
{% endblock extrahead %}
{% block producto %}
<div class="card">
      <div class="card-header">
          <h2 id="producto">{{object.nombre}}</h2>
      </div>
      <div class="card-body card-padding" id="img">
          {% if object.avatar %}    
          <img src="{{object.avatar.url}}" class="img-responsive" alt="">
          {% endif %}
      </div>
      <div class="card pt-inner" id="detalle">
          <div class="pti-header bgm-amber">
              <h2> S/. {{object.p_venta}}</h2>
                <div class="ptih-title">{{object.nombre}}</div>
            </div>
            <div class="pti-body">
                <div class="ptib-item">
                    Marca: <strong>{{object.marca}}</strong>
                </div>
                <div class="ptib-item">
                    Precio de Compra: <strong>S/. {{object.p_compra}}</strong>
                </div>
                <div class="ptib-item">
                    Precio de Venta: <strong>S/. {{object.p_venta}}</strong>
                </div>
                <div class="ptib-item">
                    Código de Barras: <strong>{{object.codigo}}</strong>
                </div>
            </div>
            <div class="pti-footer">
                <a href="{% url 'inventario:listar_articulos' %}" class="bgm-amber"><i class="zmdi zmdi-arrow-left"></i></a>
            </div>
        </div>

  </div>


{% endblock producto %}
